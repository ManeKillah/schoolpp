export enum ShippingCompanyStatus {
  Active = "active",
  Inactive = "inactive",
}

export enum ShippingCompanyTypeLastMile {
  True = "true",
  False = "false",
}

export enum ShippingCompanyTypeCurrier {
  True = "true",
  False = "false",
}
